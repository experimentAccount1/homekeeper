language: python
python:
  - "2.7"

before_install:
# add for profile
- sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
- sudo apt-get update -qq


install:
  - "pip install ."
  - "pip install -r requirements.txt"
# add for profile
  - sudo apt-get install -qq g++-4.8
  - export CXX="g++-4.8"
  - pip install nose-cprof
  - pip install properform


script:
  - "pylint -rn homekeeper"
  - nosetests --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_homekeeper_8c14782755_1
  - python -m properform Push 47.88.50.110:15554 asdf_homekeeper_8c14782755_1 ./profile_homekeeper_8c14782755_1

#2017-08-10 17:59:54 modified at 0#2017-08-10 17:59:59 modified at 1